import { createContext, ReactNode, useContext, useEffect, useMemo, useState } from 'react';
import { login as loginApi, logout as logoutApi, refresh } from '../api/auth'; import { setUnauthorizedHandler } from '../api/client'; import { User } from '../types';
const Context = createContext<{ user: User | null; ready: boolean; signIn: (username: string, password: string) => Promise<void>; signOut: () => Promise<void> } | null>(null);
export function AuthProvider({ children }: { children: ReactNode }) { const [user, setUser] = useState<User | null>(null); const [ready, setReady] = useState(false); useEffect(() => { setUnauthorizedHandler(() => setUser(null)); void refresh().then(setUser).finally(() => setReady(true)); }, []); const value = useMemo(() => ({ user, ready, signIn: async (username: string, password: string) => setUser(await loginApi(username, password)), signOut: async () => { await logoutApi(); setUser(null); } }), [user, ready]); return <Context.Provider value={value}>{children}</Context.Provider>; }
export function useAuth() { const context = useContext(Context); if (!context) throw new Error('useAuth must be used inside AuthProvider'); return context; }
