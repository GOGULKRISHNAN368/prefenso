import { useQuery } from '@tanstack/react-query';
import { ArrowUpRight, BarChart3, Building2, Clock3, LogIn, LogOut, Users } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Bar, BarChart as RechartsBarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
const BarChart = (props: any) => props.data ? <RechartsBarChart {...props}/> : <BarChart3 size={props.size}/>;
import { getDashboard } from '../api/queries';
import { Loading, SkeletonCards } from '../components/common/Loading';
import { StatusBadge } from '../components/common/StatusBadge';
import { Visitor } from '../types';
function time(value: string | null) { return value ? new Intl.DateTimeFormat('en-IN', { timeZone: 'Asia/Kolkata', hour: 'numeric', minute: '2-digit' }).format(new Date(value)) : '—'; }
function date(value: string) { return new Intl.DateTimeFormat('en-IN', { timeZone: 'Asia/Kolkata', day: '2-digit', month: 'short' }).format(new Date(value)); }
export function Dashboard() { const query = useQuery({ queryKey: ['dashboard'], queryFn: getDashboard }); if (query.isLoading) return <><div className="page-heading"><div><p className="eyebrow">OVERVIEW</p><h2>Good morning, administrator</h2></div></div><SkeletonCards/><Loading label="Loading dashboard..."/></>; if (query.isError || !query.data) return <div className="error-state"><h3>Dashboard unavailable</h3><p>We couldn’t load the latest campus activity.</p><button className="secondary-btn" onClick={() => void query.refetch()}>Try again</button></div>; const d = query.data; return <><div className="page-heading"><div><p className="eyebrow">OVERVIEW / {date(new Date().toISOString()).toUpperCase()}</p><h2>Good morning, administrator</h2><p className="muted">Here’s what’s happening across your campus today.</p></div><Link className="primary-btn" to="/visitors"><Users size={18}/> View all visitors</Link></div><div className="stats-grid"><Stat icon={<Users/>} label="Visitors today" value={d.visitorsToday} detail="All campus entries" tone="blue"/><Stat icon={<Clock3/>} label="Currently inside" value={d.inside} detail="Live occupancy" tone="amber"/><Stat icon={<LogOut/>} label="Exited today" value={d.exitedToday} detail="Completed visits" tone="green"/><Stat icon={<Building2/>} label="Active blocks" value={d.activeBlocks} detail="Ready for visitors" tone="purple"/></div><div className="dashboard-grid"><section className="panel chart-panel"><div className="panel-heading"><div><p className="eyebrow">LAST 7 DAYS</p><h3>Visitor activity</h3></div><span className="chart-key"><i/> Arrivals</span></div><div className="chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={d.activity} barSize={18}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="#e2e8f0"/><XAxis dataKey="date" tickFormatter={(v) => v.slice(5)} axisLine={false} tickLine={false} tick={{ fill: '#64748b', fontSize: 12 }}/><YAxis allowDecimals={false} axisLine={false} tickLine={false} tick={{ fill: '#64748b', fontSize: 12 }}/><Tooltip cursor={{ fill: '#f1f5f9' }}/><Bar dataKey="visitors" fill="#2563eb" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="panel quick-panel"><div className="panel-heading"><div><p className="eyebrow">QUICK ACTIONS</p><h3>Keep things moving</h3></div></div><Link className="quick-link" to="/people-inside"><span className="quick-icon amber"><LogIn size={19}/></span><span><strong>People inside</strong><small>See active visitors by block</small></span><ArrowUpRight size={17}/></Link><Link className="quick-link" to="/blocks"><span className="quick-icon blue"><Building2 size={19}/></span><span><strong>Manage blocks</strong><small>Configure access credentials</small></span><ArrowUpRight size={17}/></Link><Link className="quick-link" to="/reports"><span className="quick-icon green"><BarChart size={19}/></span><span><strong>View reports</strong><small>Understand campus patterns</small></span><ArrowUpRight size={17}/></Link></section></div><section className="panel table-panel"><div className="panel-heading"><div><p className="eyebrow">LATEST ACTIVITY</p><h3>Recent visitors</h3></div><Link className="text-link" to="/visitors">View all <ArrowUpRight size={15}/></Link></div><div className="table-scroll"><table><thead><tr><th>Visitor</th><th>Block</th><th>Check-in</th><th>Check-out</th><th>Status</th></tr></thead><tbody>{d.recentVisitors.length ? d.recentVisitors.map((visitor: Visitor) => <tr key={visitor.id}><td><div className="person-cell"><span className="person-avatar">{visitor.visitorName.slice(0,1).toUpperCase()}</span><span><strong>{visitor.visitorName}</strong><small>{visitor.visitorCode}</small></span></div></td><td><strong>{visitor.block?.name ?? '—'}</strong><small className="cell-sub">{visitor.block?.code}</small></td><td>{time(visitor.checkInAt)}</td><td>{time(visitor.checkOutAt)}</td><td><StatusBadge status={visitor.status}/></td></tr>) : <tr><td colSpan={5}><div className="empty-state">No visitors recorded today.</div></td></tr>}</tbody></table></div></section></>; }
function Stat({ icon, label, value, detail, tone }: { icon: React.ReactNode; label: string; value: number; detail: string; tone: string }) { return <div className="stat-card"><div className={`stat-icon ${tone}`}>{icon}</div><div><p>{label}</p><strong>{value}</strong><small>{detail}</small></div></div>; }
