import { FormEvent, useState } from 'react';
import { Eye, EyeOff, LockKeyhole, ShieldCheck, UserRound } from 'lucide-react';
import { toast } from 'sonner';
import { useAuth } from '../hooks/useAuth';
import { apiError } from '../api/client';
export function Login() { const { signIn } = useAuth(); const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [show, setShow] = useState(false); const [loading, setLoading] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); if (!username.trim() || !password) { toast.error('Enter your username and password'); return; } setLoading(true); try { await signIn(username, password); toast.success('Welcome back'); } catch (error) { toast.error(apiError(error)); } finally { setLoading(false); } }; return <div className="login-page"><div className="login-decoration"><div className="decoration-grid"/><div className="login-story"><div className="brand-mark large"><ShieldCheck size={28}/></div><p className="eyebrow light">GATEWISE / ADMINISTRATION</p><h1>Every arrival accounted for.</h1><p>One secure command center for your campus visitor operations.</p><div className="login-stat-row"><span><strong>6+</strong><small>Campus blocks</small></span><span><strong>24/7</strong><small>Gate coverage</small></span></div></div></div><div className="login-panel"><div className="mobile-brand"><div className="brand-mark"><ShieldCheck size={21}/></div><strong>Gatewise</strong></div><div className="login-card"><div className="login-heading"><div className="brand-mark small"><ShieldCheck size={20}/></div><p className="eyebrow">SECURE ADMIN ACCESS</p><h2>Sign in to your console</h2><p>Manage blocks, visitors, and campus access from one place.</p></div><form onSubmit={submit} className="form-stack"><label>Username or email<div className="input-wrap"><UserRound size={18}/><input value={username} onChange={(e) => setUsername(e.target.value)} autoComplete="username" placeholder="Enter your username"/></div></label><label>Password<div className="input-wrap"><LockKeyhole size={18}/><input type={show ? 'text' : 'password'} value={password} onChange={(e) => setPassword(e.target.value)} autoComplete="current-password" placeholder="Enter your password"/><button type="button" className="input-action" onClick={() => setShow(!show)} aria-label={show ? 'Hide password' : 'Show password'}>{show ? <EyeOff size={18}/> : <Eye size={18}/>}</button></div></label><label className="checkbox-line"><input type="checkbox" defaultChecked/> <span>Remember this device</span></label><button className="primary-btn full-btn" disabled={loading}>{loading ? 'Signing in...' : 'Sign in to admin console'}<span>→</span></button></form><p className="login-footnote">Access is protected with secure session controls.</p></div></div></div>; }
